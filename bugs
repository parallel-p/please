
ИСПРАВИТЬ

если нет в конфиге конкретного солюшна, при попытке его прочекать вываливается traceback

пофиксить тесты bad_tl - bad_ml - ...

Лог компилятора вида: ___.cpp compiled / ___.exe is up-to-date and needs no recompile

Проверять, что pdflatex отработал успешно, и выводить соотв. сообщения (Пыгорь)

+ Написать help к новому add solution (Журавлев)

При импорте из polygon'а некорректно импортируется для решения вердикт rejected (метод решения без большого увеличения функциональности не найден)

+ Если в stdout или stderr не валидный utf8 - всё падает

compile cache - не по дате? (Дубов -> Григорьев)

import from polygon: не прописан в большой хелп

+ import from polygon: не добавляются все решения

+ auto_TL: текущий превратить в integer, и сделать произвольный с min 0,1. Округлять вверх!

+ в README указать, что нужны соотв ssh-утилиты

научить import from polygon скачивать контест самому, а не работать с готовым архивом
                    
language_configurator: проверить ВСЁ про linux. Какой-то редиска не проверил get_binary_name. Вот.

Если у пользователя не установлен нужный комплиятор, compiler.py падает со страшным exception

Java работает? (dominoes)

+ check solution - TL и сообщения чекера

исправить [] в help

export from polygon: сначала прописывать время создания (возможно, -1 сек от реального) и лишь потом все создавать,
иначе todo считает, что всё default

В please/tests_answer_generator/tests_answer_generator.py при генерации тестов основным решением получается
дефолтное ограничение на память (а не то, что указано в глобальном конфиге). Это баг. Невозможно указать
отдельно ограничения на запуск программы и на копмиляцию. Это ботва.

export to ejudge сделать кросс-платформенным

Не надо класть testlib.h в каждую задачу. Эту проблему надо решить адекватным способом.

ИСПРАВЛЕНО
+ import from polygon: не генерятся тесты

+ help: если command line не соответствует шаблону, тоже выводить help

+ генерация условий: вместо 2s писать "2 секунды"

+ В solution_tester.package_config.PackageConfig.get_config не закрывается файл

+ В executors.compiler.compile не делать str(stdout), а использовать stdout.decode()

+ Команда please generate tests падает, тестов для неё не написано

+ todo: почему иногда SOLUTION, а иногда PATH_TO_SOLUTION?

+ invoker: посмотреть, как работает invoker с переводами строк при перенаправлении

+ generate statement: не работает в задаче

+ generate statement: убрать подчеркивание из названия контеста

+ export from ejudge: не указана кодировка в open, строка 18

+ export from ejudge: баги с папочкой .svn

+export from ejudge: в please-команде task заменить на problem для единообразия 

